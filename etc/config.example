module GPIO
  LOCK = 31
  UNLOCK = 30
  LOCKED = 48
  UNLOCKED = 60
  OPENED = 51
end
module Daemon
  PORT = 1337
  UID = "sentry"
  GID = "sentry"
  KEY = "/etc/sentry/privkey.pem"
  CERT = "/etc/sentry/cert.pem"
  CLIENTS = "/etc/sentry/clients"
  SECRETS = "/etc/sentry/secrets"
  LOG = STDERR
  LOG_LEVEL = Logger::DEBUG
end
